import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerPrice: {
        width: "100%",
        height: "auto",
        alignItems: "center",
        marginBottom: 10
    },
    currentPrice: {
        fontWeight: "bold",
        fontSize: 22,
        color: "#6f4fe5",
    },
    textPrice: {
        fontSize: 16,
        fontWeight: "bold",

    }
});

export default styles